# Sentinel — Build Plan

## Goal
Build the Sentinel MCP server + CLI: NIST AI RMF / FS AI RMF compliance learning and checking tool.

## Current Milestone
COMPLETE — All phases delivered.

## Status
All 5 phases complete. v0.4.0 deployed. Standalone Supabase account. Signup page live at my2b.ai/sentinel. Study guide with live API drill-down. 4 PRs merged on Sentinel repo, 7 PRs merged on my2b.ai.

## Completed Milestones

### Phase 1: Foundation
Supabase project, schema with RLS on all tables, auth-hardened edge function, MCP handshake tested. License switched from AGPL to MIT.

### Phase 2: Data Ingestion
408+ rows seeded: NIST taxonomy (4 functions, 18 categories, 67 subcategories, 7 trustworthiness chars), 230 FS AI RMF controls with financial services context, 82 glossary terms, 109 crosswalks (SR 11-7, ISO 42001, EU AI Act, OWASP LLM).

### Phase 3: Learning Tools
5 tools implemented: lookup (hierarchical ID + FTS), explain (data-driven multi-table), glossary (category + term), quiz (3 types + SM-2 flashcard tracking + auto-seed 70+ cards), progress (stats dashboard).

### Phase 4: Compliance Tools
3 tools implemented: assess (keyword extraction + relevance scoring against 230 controls), crosswalk (109 entries across 4 frameworks), adoption_stage (5-dimension maturity scoring).

### Phase 5: Polish + Auth Hardening
- README, CHANGELOG, SECURITY.md, data sources docs
- 41 unit tests passing
- Rate limiting (60/min tools/call, 10/min discovery)
- Audit logging (every call: user, tool, IP, duration, status)
- 4-layer auth: HTTPS -> gateway (HS256 anon key) -> X-Sentinel-Token (ES256 user JWT) -> RLS
- Password policy: 10+ chars, upper+lower+digit+symbol, email confirmation
- Signup page at my2b.ai/sentinel with credential display + config generators
- CLI with local SQLite + remote sync
- Migrated to standalone Supabase account (ewugluzfpgsonifbpeau, free tier, transferable)
- Study guide cards with cross-references + live Sentinel API drill-down (18 cards)
- Auto-seed flashcards for new users
- Code review fixes: XSS prevention, null safety, input sanitization, race condition guards
